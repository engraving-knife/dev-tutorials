# HTML 目录实时监控服务 —— 设计思路与架构说明

## 一、需求分析

1. **可部署的 Web 服务**：Java 11 实现，能在云服务器上独立运行。
2. **实时监测本地目录**：监测部署侧本地的一个目录及其子目录中的 HTML 文件，
   文件的新增、修改、删除需被实时感知。
3. **HTML 文件映射到前端**：将本地目录结构映射到前端展示成一个目录树，
   点击其中的 HTML 文件可原样预览该 HTML 页面。
4. **目录结构吻合**：前端展示的目录层级、名称与本地目录一一对应。

## 二、技术选型

| 需求点 | 选型 | 理由 |
| --- | --- | --- |
| Web 服务 | JDK 内置 `com.sun.net.httpserver.HttpServer` | 零第三方依赖，fat jar 一键运行，部署简单 |
| 实时目录监听 | `java.nio.file.WatchService` | JDK 原生文件事件机制，系统级实时回调 |
| 目录树数据 | 内存快照 + JSON 接口 | 请求零磁盘扫描，展示性能好 |
| 前端展示 | 原生 HTML/CSS/JS | 无构建链，内嵌于 jar，部署即用 |
| 构建打包 | Maven + shade 插件 | 产出可执行 fat jar，适配云服务器 |

选型核心原则：**全链路 JDK 自足，无任何第三方依赖**，从源码到可执行产物
链路最短，云服务器上只要有 JDK 11 即可运行。

## 三、整体架构

```
                          ┌──────────────────────────────────────┐
                          │           HtmlMonitorApplication     │
                          │              （启动编排）              │
                          └───┬──────────────┬───────────────┬───┘
                              │              │               │
                     ┌────────▼────┐  ┌──────▼───────┐  ┌────▼─────────┐
                     │  Directory  │  │   FileIndex  │  │ HttpServer   │
                     │  Watcher    │──▶│  （内存快照）  │◀─│ Manager      │
                     │  WatchService│  └──────┬───────┘  └──────┬───────┘
                     └────────┬────┘         │                 │
                              │              │           ┌─────▼──────────────┐
                     ┌────────▼────┐  实时同步 │    ┌──────┴─────┐  ┌──────────┴──┐
                     │  本地监控目录  │──────────┘    │ /api/files │  │  /html/*    │
                     │  （html 文件）│                │ 目录树 JSON │  │ 原始内容     │
                     └─────────────┘                └────────────┘  └─────────────┘
```

## 四、模块说明

| 模块 | 类 | 职责 |
| --- | --- | --- |
| 入口 | `HtmlMonitorApplication` | 配置加载、组件组装与启动编排 |
| 配置 | `config/ServerConfig` | 读取 `application.properties`，管理地址/端口/监控目录 |
| 模型 | `model/FileNode` | 目录树节点（目录/文件、路径、大小、修改时间） |
| 监控 | `monitor/DirectoryWatcher` | WatchService 递归监听，事件驱动快照重建 |
| 索引 | `monitor/FileIndex` | 维护内存目录树快照，volatile 无锁发布 |
| 服务 | `server/HttpServerManager` | 创建 HttpServer、注册路由、管理线程池 |
| 处理 | `server/handler/ApiHandler` | `/api/files` 返回目录树 JSON |
| 处理 | `server/handler/HtmlContentHandler` | `/html/*` 原样返回本地文件内容（含越界防护） |
| 处理 | `server/handler/StaticHandler` | 返回内嵌前端页面 `index.html` |
| 前端 | `resources/static/index.html` | 目录树渲染、3 秒轮询刷新、iframe 预览 |

## 五、核心链路设计

### 1. 实时监测链路（文件变化 → 快照更新）

```
本地文件增/删/改
      │  WatchService 系统事件
      ▼
DirectoryWatcher.runLoop
      │  事件批内只重建一次
      ▼
FileIndex.rebuild()  ──▶ 全量扫描磁盘，重建目录树快照
      │  volatile 引用替换，无锁
      ▼
/api/files 接口读到最新快照
      │  前端每 3 秒轮询
      ▼
目录树自动刷新
```

> **为何用全量重建而非增量合并**：本地目录规模通常可控（几十到几万文件），
> 全量扫描耗时毫秒级；而增量合并需要处理重命名、删除、嵌套目录等大量边界
> 情况，复杂度高、易出错。用可控的扫描成本换取实现简单与逻辑可靠。

> **实时性的界定**：磁盘侧为操作系统级事件（毫秒级实时）；前端侧采用 3 秒
> 轮询拉取快照，页面感知延迟不超过 3 秒，对监控展示场景足够实时。轮询方式
> 还天然兼容反向代理与防火墙环境，无需长连接。

### 2. HTML 映射链路（点击文件 → 展示原页面）

```
前端目录树点击文件节点
      ▼
iframe.src = /html/<相对路径>
      ▼
HtmlContentHandler
      │  ① 相对路径 normalize 后校验仍在监控根目录内（防路径穿越）
      │  ② 探测 HTML 声明的 charset，设置正确的 Content-Type
      ▼
原样输出本地文件字节
      ▼
浏览器 iframe 展示原始 HTML（相对资源引用亦可正常加载）
```

### 3. 两级目录结构设计

- `FileIndex` 构建的目录快照为**两级结构**：
  - 第一级：监控根目录下的第一级子目录名，仅保留其中含 HTML 文件的目录；
    根目录下直接存放的 HTML 文件归入以根目录命名的分组，保证不遗漏；
  - 第二级：该目录下递归收集的全部 HTML 文件（含深层子目录），节点携带
    完整相对路径，可直接用于 `/html/` 预览；
- 每个文件节点的 `relativePath` 以 `/` 分隔、相对监控根目录，前端渲染的
  两级分组与本地目录一一对应；

## 六、接口说明

| 路径 | 方法 | 说明 |
| --- | --- | --- |
| `/` | GET | 前端展示页面 |
| `/api/files` | GET | 目录树 JSON（`{name,path,directory,size,lastModified,children[]}`） |
| `/html/<相对路径>` | GET | 返回监控目录内文件的原始内容 |

## 七、部署与运行

### 本地 / 云服务器运行

```bash
# 1. 打包（要求 JDK 11 + Maven）
mvn -DskipTests package

# 2. 运行（产物 target/html-monitor.jar）
java -jar target/html-monitor.jar
```

- 默认监控目录为启动目录下的 `html-files`（不存在会自动创建）；
- 通过 `src/main/resources/application.properties` 修改端口与监控目录：
  - `server.host`：监听地址，云服务器保持 `0.0.0.0`；
  - `server.port`：服务端口；
  - `monitor.dir`：监控目录，支持相对/绝对路径；
- 浏览器访问 `http://<服务器IP>:8080` 查看目录树并预览 HTML。

### 云服务器进程守护（示例）

```bash
# systemd 服务文件 /etc/systemd/system/html-monitor.service
[Unit]
Description=HTML Monitor
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/html-monitor
ExecStart=/usr/bin/java -jar /opt/html-monitor/html-monitor.jar
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now html-monitor
```

## 八、扩展点

- **实时推送**：将前端轮询替换为 SSE（`/api/events`）或 WebSocket，可在文件
  变化瞬间推送目录树刷新，适合超大目录或对实时性要求更高的场景；
- **目录过滤**：在 `FileIndex` 中增加忽略规则（如隐藏目录、`.git` 等）；
- **多目录监控**：将单根目录扩展为多根目录，每个根目录一个 `DirectoryWatcher`
  与 `FileIndex`；
- **安全加固**：`/html/*` 已做路径越界防护；如需鉴权可增加登录拦截器。
