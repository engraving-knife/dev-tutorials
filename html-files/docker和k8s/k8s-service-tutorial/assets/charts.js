// Generated by Trae Work
(function() {
  var style = getComputedStyle(document.documentElement);
  var accent = style.getPropertyValue('--accent').trim();
  var accent2 = style.getPropertyValue('--accent2').trim();
  var ink = style.getPropertyValue('--ink').trim();
  var muted = style.getPropertyValue('--muted').trim();
  var rule = style.getPropertyValue('--rule').trim();
  var bg2 = style.getPropertyValue('--bg2').trim();

  var proxyEl = document.getElementById('chart-proxy');
  if (proxyEl) {
    var proxyChart = echarts.init(proxyEl, null, { renderer: 'svg' });
    proxyChart.setOption({
      animation: false,
      color: [accent, accent2],
      tooltip: {
        trigger: 'axis',
        appendToBody: true,
        formatter: function(params) {
          var lines = params.map(function(p) {
            return p.marker + ' ' + p.seriesName + ': <b>' + p.value + '</b> ms';
          });
          return params[0].axisValue + ' 个 Service<br/>' + lines.join('<br/>');
        }
      },
      legend: { data: ['iptables', 'IPVS'], top: 0, textStyle: { color: muted } },
      grid: { left: 52, right: 24, top: 44, bottom: 46 },
      xAxis: {
        type: 'category',
        data: ['100', '500', '1000', '2000', '5000', '10000'],
        name: 'Service 数量',
        nameTextStyle: { color: muted },
        axisLine: { lineStyle: { color: rule } },
        axisLabel: { color: muted },
        axisTick: { show: false }
      },
      yAxis: {
        type: 'log',
        name: '匹配延迟 (ms)',
        nameTextStyle: { color: muted },
        axisLine: { show: false },
        axisLabel: { color: muted },
        splitLine: { lineStyle: { color: rule } }
      },
      series: [
        {
          name: 'iptables',
          type: 'line',
          smooth: true,
          symbol: 'circle',
          symbolSize: 7,
          data: [0.5, 0.9, 1.4, 2.4, 6.0, 15.0],
          lineStyle: { width: 3 },
          itemStyle: { color: accent },
          areaStyle: { color: accent, opacity: 0.08 }
        },
        {
          name: 'IPVS',
          type: 'line',
          smooth: true,
          symbol: 'diamond',
          symbolSize: 8,
          data: [0.20, 0.25, 0.32, 0.42, 0.70, 1.10],
          lineStyle: { width: 3 },
          itemStyle: { color: accent2 },
          areaStyle: { color: accent2, opacity: 0.08 }
        }
      ]
    });
    window.addEventListener('resize', function() { proxyChart.resize(); });
  }
})();