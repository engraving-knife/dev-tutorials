/* Generated by Trae Work — Flink YARN 架构图表逻辑 */
(function () {
  var style = getComputedStyle(document.documentElement);
  var accent = style.getPropertyValue('--accent').trim();
  var accent2 = style.getPropertyValue('--accent2').trim();
  var ink = style.getPropertyValue('--ink').trim();
  var muted = style.getPropertyValue('--muted').trim();
  var rule = style.getPropertyValue('--rule').trim();
  var bg2 = style.getPropertyValue('--bg2').trim();
  var gradFrom = '#14d3c8';
  var gradTo = '#7c6bff';

  /* ---- 图4：Slot 共享对比（并行度 4，5 算子链） ---- */
  var chartSlots = document.getElementById('chart-slots');
  if (chartSlots) {
    var slots = echarts.init(chartSlots, null, { renderer: 'svg' });
    slots.setOption({
      animation: false,
      tooltip: { appendToBody: true, trigger: 'axis', axisPointer: { type: 'shadow' } },
      legend: { data: ['开启 Slot 共享', '关闭 Slot 共享'], textStyle: { color: muted }, top: 0 },
      grid: { left: 60, right: 40, top: 50, bottom: 40 },
      xAxis: {
        type: 'category',
        data: ['并行度 4\n(5 算子链条)', '并行度 8\n(5 算子链条)', '并行度 16\n(5 算子链条)'],
        axisLine: { lineStyle: { color: rule } },
        axisLabel: { color: muted, fontSize: 12 },
      },
      yAxis: {
        type: 'value', name: '所需 Slot 数',
        axisLine: { lineStyle: { color: rule } },
        axisLabel: { color: muted },
        splitLine: { lineStyle: { color: rule } },
      },
      series: [
        {
          name: '开启 Slot 共享', type: 'bar', barWidth: 26,
          itemStyle: { color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [{ offset: 0, color: gradFrom }, { offset: 1, color: gradTo }]), borderRadius: [6, 6, 0, 0] },
          data: [4, 8, 16],
        },
        {
          name: '关闭 Slot 共享', type: 'bar', barWidth: 26,
          itemStyle: { color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [{ offset: 0, color: 'rgba(20,211,200,0.28)' }, { offset: 1, color: 'rgba(124,107,255,0.28)' }]), borderRadius: [6, 6, 0, 0] },
          data: [20, 40, 80],
        },
      ],
    });
    window.addEventListener('resize', function () { slots.resize(); });
  }

  /* ---- 图5：YARN 三种模式对比 ---- */
  var chartModes = document.getElementById('chart-modes');
  if (chartModes) {
    var modes = echarts.init(chartModes, null, { renderer: 'svg' });
    modes.setOption({
      animation: false,
      tooltip: { appendToBody: true, trigger: 'axis', axisPointer: { type: 'shadow' } },
      legend: { data: ['Session 模式', 'Per-Job 模式', 'Application 模式'], textStyle: { color: muted }, top: 0 },
      radar: {
        indicator: [
          { name: 'JobManager 个数', max: 10 },
          { name: 'TaskManager 共享', max: 10 },
          { name: '作业间隔离', max: 10 },
          { name: '冷启动速度', max: 10 },
          { name: '资源利用率', max: 10 },
        ],
        radius: '62%',
        axisName: { color: muted, fontSize: 11 },
        splitLine: { lineStyle: { color: rule } },
        splitArea: { areaStyle: { color: ['rgba(255,255,255,0.4)', 'rgba(14,165,164,0.05)'] } },
        axisLine: { lineStyle: { color: rule } },
      },
      series: [
        {
          type: 'radar',
          data: [
            { value: [10, 2, 3, 10, 8], name: 'Session 模式', itemStyle: { color: accent, borderColor: accent } },
            { value: [3, 8, 8, 6, 7], name: 'Per-Job 模式', itemStyle: { color: accent2, borderColor: accent2 } },
            { value: [3, 10, 10, 7, 6], name: 'Application 模式', itemStyle: { color: '#0e7fb5', borderColor: '#0e7fb5' } },
          ],
          areaStyle: { opacity: 0.12 },
        },
      ],
    });
    window.addEventListener('resize', function () { modes.resize(); });
  }
})();