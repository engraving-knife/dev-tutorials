/* Generated by Trae Work */
(function () {
  var style = getComputedStyle(document.documentElement);
  var accent = style.getPropertyValue('--accent').trim();
  var accent2 = style.getPropertyValue('--accent2').trim();
  var accent3 = style.getPropertyValue('--accent3').trim();
  var ink = style.getPropertyValue('--ink').trim();
  var muted = style.getPropertyValue('--muted').trim();
  var rule = style.getPropertyValue('--rule').trim();
  var bg2 = style.getPropertyValue('--bg2').trim();

  // --- Chart 1: 主流流处理引擎能力雷达对比（示意） ---
  var el1 = document.getElementById('chart-radar');
  if (el1) {
    var radar = echarts.init(el1, null, { renderer: 'svg' });
    radar.setOption({
      animation: false,
      color: [accent, accent2, '#f59e0b', accent3],
      tooltip: { trigger: 'item', appendToBody: true },
      legend: {
        bottom: 0,
        textStyle: { color: muted, fontFamily: 'WorkSans, PingFang SC, sans-serif' }
      },
      radar: {
        indicator: [
          { name: '延迟', max: 10 },
          { name: '吞吐', max: 10 },
          { name: '状态支持', max: 10 },
          { name: '容错一致性', max: 10 },
          { name: '乱序处理', max: 10 },
          { name: '生态丰富度', max: 10 }
        ],
        radius: '62%',
        center: ['50%', '48%'],
        splitNumber: 4,
        shape: 'polygon',
        axisName: { color: ink, fontFamily: 'WorkSans, PingFang SC, sans-serif', fontSize: 11 },
        splitArea: { areaStyle: { color: ['transparent'] } },
        splitLine: { lineStyle: { color: rule } },
        axisLine: { lineStyle: { color: rule } }
      },
      series: [{
        type: 'radar',
        data: [
          { value: [9.5, 8, 9.5, 9.7, 9.6, 9.2], name: 'Flink', areaStyle: { color: accent + '55' }, lineStyle: { width: 2, color: accent } },
          { value: [6, 7.5, 7, 7.5, 8.8, 8.5], name: 'Spark', areaStyle: { color: accent2 + '40' }, lineStyle: { width: 2, color: accent2 } },
          { value: [7.5, 6, 5, 6, 4, 6.5], name: 'Storm', areaStyle: { color: '#f59e0b' + '40' }, lineStyle: { width: 2, color: '#f59e0b' } },
          { value: [9, 9.6, 8.5, 9, 9, 7.5], name: 'Kafka Streams', areaStyle: { color: accent3 + '40' }, lineStyle: { width: 2, color: accent3 } }
        ]
      }]
    });
    window.addEventListener('resize', function () { radar.resize(); });
  }
})();