# 注释规范（Java 项目）

> 适用范围：本项目 `html-monitor` 全部 Java 源文件。
> 语言要求：所有注释一律使用中文。

## 1. 文件级注释（类注释）

每个类 / 接口上方必须有文件级注释块，说明：
- 该类的职责（做什么）；
- 设计意图（为什么这样设计，关键取舍）。

格式：

```java
/**
 * 类职责的一句话说明。
 *
 * 设计意图：
 * 1. 设计要点一及其原因；
 * 2. 设计要点二及其原因。
 */
public class Xxx { ... }
```

## 2. 方法级注释（函数注释）

每个方法上方必须有方法级注释，说明：
- 该方法做什么；
- 入参、返回值语义；
- 特殊设计点（若有必要）。

格式：

```java
/**
 * 方法职责的一句话说明。
 *
 * @param 参数名 参数语义说明
 * @return 返回值语义说明
 * @throws 异常类型 抛出场景说明
 */
public ReturnType methodName(ParamType param) { ... }
```

私有工具方法同样需要注释；构造方法可省略或合并进类注释。

## 3. 关键行注释

仅对“逻辑不自明”的代码行添加行注释，解释意图而非复述代码，例如：

```java
// reset 返回 false 表示该目录已被删除，从登记表移除
if (!key.reset()) {
    registeredKeys.remove(key);
}
```

禁止对显而易见的三行内代码加无意义注释。

## 4. 命名规范

- 类名：大驼峰（PascalCase），如 `DirectoryWatcher`；
- 方法名 / 变量名：小驼峰（camelCase），如 `registerRecursively`；
- 常量：全大写下划线分隔，如 `CHARSET_SCAN_BYTES`；
- 包名：全小写，如 `com.example.htmlmonitor.monitor`。

## 5. 注释示例

```java
/**
 * 目录实时监听器，基于 JDK 的 java.nio.file.WatchService 实现。
 *
 * 设计意图：
 * 1. 递归注册根目录及其全部子目录，保证嵌套目录内的文件变化也能被感知；
 * 2. 事件到达后触发 FileIndex 全量重建，实现本地文件变化实时同步到前端。
 */
public class DirectoryWatcher { ... }
```

## 6. 其他约定

- 禁止使用 `// TODO 待实现` 代替真实实现；
- 文档注释使用 `/** ... */`，行注释使用 `//`；
- 注释与代码同语言（中文），不混用英文缩写（专有名词除外，如 API、HTTP）。
